# =============================================================================
# Trame Environment Configuration
# =============================================================================
#
# This file documents all available environment variables.
# See README.md for detailed setup instructions.
#
# Quick start:
#   - For local dev (no Docker): Copy this to .env and run `cargo run`
#   - For Docker dev:  Use .env.dev (already configured)
#   - For Docker prod: Copy .env.prod and set ALLOWED_ORIGIN
#
# =============================================================================

# Server Configuration
# -----------------------------------------------------------------------------
PORT=3000                    # Port the server listens on
HOST=127.0.0.1               # Bind address (use 0.0.0.0 in Docker)

# Database
# -----------------------------------------------------------------------------
DATABASE_URL=trame.db        # SQLite file path (relative or absolute)

# Security
# -----------------------------------------------------------------------------
ALLOWED_ORIGIN=*             # CORS: * for dev, https://yourdomain.com for prod

# Logging (optional)
# -----------------------------------------------------------------------------
RUST_LOG=info                # Log level: error, warn, info, debug, trace
